#include <iostream>
#include <string>
#include <list>

using namespace std;

int main(){
	list<char> editor;
	string input;
	int cursor;
	
	getline(cin, input);
	for(int i = 0; i < input.size(); i++){
		editor.push_back(input[i]);
	}
	
	list<char>::iterator iter = editor.begin();
	iter++;
	iter++;
	iter--;
	iter++;
	editor.insert(iter, 'h');
	
	
	for(iter = editor.begin(); iter!= editor.end(); iter++){
        cout <<*iter;
    }    
    cout << endl;
}
